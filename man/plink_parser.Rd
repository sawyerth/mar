% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fileparser.R
\name{plink_parser}
\alias{plink_parser}
\title{Convert PLINK binary files to GDS format}
\usage{
plink_parser(plink.fn)
}
\arguments{
\item{plink.fn}{Base name of PLINK files (without .bed/.bim/.fam extension)}

\item{gds.fn}{Optional. Path for the output GDS file. If NULL, will use the same name as PLINK files
with .gds extension}

\item{opengds}{Logical. If TRUE, opens and returns the GDS file handle. If FALSE, returns the
path to the created GDS file. Default is FALSE}
}
\value{
If opengds=TRUE, returns an opened GDS file connection. If opengds=FALSE, returns the
path to the created GDS file as a character string
}
\description{
This function converts PLINK binary files (.bed, .bim, .fam) to GDS (Genomic Data Structure) format
using SeqArray package.
}
\examples{
\dontrun{
# Convert PLINK files to GDS
gds_file <- plink_parser("dataset")

# Convert and open GDS file
gds_conn <- plink_parser("dataset", opengds = TRUE)
}
}
